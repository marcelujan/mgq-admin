# 05_FRONTEND

## Navegación principal actual

El header principal expone las hojas de trabajo en este orden:

- `/items` (Items)
- `/items-manuales` (Items Manuales)
- `/productos` (Items Formulados)
- `/items-proveedores` (Items Proveedores)
- `/items-comerciales` (Items Comerciales)
- `/items-envases` (Items Envases)
- `/items-etiqueta` (Items Etiqueta)
- `/items-paqueteria` (Items Paquetería)
- `/dolar-historico` (Dólar Histórico)
- `/jobs` (Jobs manual)

## DB health

- Se muestra un indicador DB (verde/rojo) en el header.
- No es clickeable.
- La hoja técnica `/db-health` existe, pero no está en el menú principal.
