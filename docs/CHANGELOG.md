# CHANGELOG

## v31

- header superior sin scroll horizontal; banda compacta por texto y wrap en varias filas;
- `DB health` se mantiene a la derecha;
- `Items Envases`, `Items Etiqueta` e `Items Paquetería` pasan a ser catálogos autónomos;
- formularios corregidos: ya no piden `manual_cost_option_id` ni `proveedor_item_id`;
- APIs de catálogos operativos simplificadas al nuevo modelo;
- se agrega migración correctiva posterior a `2026_04_14_item_comercial_vnext.sql`.
