# CHANGELOG

## v84
- La UI pasa de mostrar `Publicaciones` a mostrar `Canales de venta`.
- Se mantiene sin cambios la ruta `/publicaciones` para evitar refactors más riesgosos.
- Se retira el bloque `Pricing por canal` del formulario principal de `Item Comercial`.
- Se fija que el precio final por canal pertenece a la capa de `Canales de venta`, no al flujo principal de `Item Comercial`.
