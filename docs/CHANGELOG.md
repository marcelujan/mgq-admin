# CHANGELOG

- v46: fixed `item_comercial` origin UOM resolution to use real schema columns (`manual_uom` in `cost_option`; `PROVEEDOR` and `FORMULADO` resolved as `GR`).
- v46: patch ZIPs now use direct `src/...` and `docs/...` structure with no extra `buildXX/` wrapper.
